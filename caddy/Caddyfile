# ROLES DEFINITION

(admin) {
	authorize with adminpolicy
}

(rowm) {
	authorize with rowmpolicy
}

(superuser) {
	authorize with superuserpolicy
}

(family) {
	authorize with familypolicy
}

(user) {
	authorize with userpolicy
}

# SERVER DEFAULTS

{
    order authenticate before respond
    order authorize before basicauth

    on_demand_tls {
        ask {env.TLS_PERMISSION_ENDPOINT}
    }	

    acme_dns porkbun {
        api_key {env.PORKBUN_API_KEY}
        api_secret_key {env.PORKBUN_SECRET_KEY}	
    }

    security {
		oauth identity provider google {
			realm google
			driver google
			client_id {env.OAUTH_GOOGLE_CLIENT_ID}
			client_secret {env.OAUTH_GOOGLE_CLIENT_SECRET}
			scopes openid email profile
		}

      authentication portal myportal {
			crypto default token lifetime 3600
            crypto key sign-verify {env.JWT_SHARED_KEY}

            enable identity provider google

			cookie domain thenairn.com
            cookie path /
			ui {
				links {
					"My Identity" "/whoami" icon "las la-user"
				}
			}

			transform user {
				match realm google
				action add role authp/user
			}

            transform user {
				match realm google
                match email thomas@thenairn.com
                action add role authp/admin
            }

            transform user {
				match realm google
                match email thomas@rowm.co
                action add role authp/admin
            }

            transform user {
				match realm google
                match email grace.nairn@gmail.com
                action add role authp/superuser
            }

            transform user {
                match realm google
                match email francesca.nairn@gmail.com
                action add role authp/family
            }

            transform user {
                match realm google
                match email nikkihazell@yahoo.co.uk
                action add role authp/family
            }
		}

		authorization policy adminpolicy {
            set auth url "https://auth.thenairn.com/oauth2/google/authorization-code-callback"
			crypto key verify {env.JWT_SHARED_KEY}
			allow roles authp/admin
			validate bearer header
			inject headers with claims
		}

		authorization policy superuserpolicy {
            set auth url "https://auth.thenairn.com/oauth2/google/authorization-code-callback"
			crypto key verify {env.JWT_SHARED_KEY}
			allow roles authp/admin authp/superuser
			validate bearer header
			inject headers with claims
		}

		authorization policy userpolicy {
            set auth url "https://auth.thenairn.com/oauth2/google/authorization-code-callback"
			crypto key verify {env.JWT_SHARED_KEY}
			allow roles authp/admin authp/superuser authp/user
			validate bearer header
			inject headers with claims
		}

		authorization policy familypolicy {
            set auth url "https://auth.thenairn.com/oauth2/google/authorization-code-callback"
			crypto key verify {env.JWT_SHARED_KEY}
			allow roles authp/admin authp/family authp/user
			validate bearer header
			inject headers with claims
		}

		authorization policy rowmpolicy {
            set auth url "https://auth.thenairn.com/oauth2/google/authorization-code-callback"
			crypto key verify {env.JWT_SHARED_KEY}
			allow roles authp/admin authp/rowm
			validate bearer header
			inject headers with claims
		}
    }
}

(proxy_error_page) {
    handle_errors {
        header Content-Type text/html
        respond <<HTML
            <!DOCTYPE html>
            <html><head>
            <meta name="viewport" content="width=device-width,initial-scale=1">
            <style>
            body{font-family:-apple-system,system-ui,sans-serif;background:#0a0a0a;color:#e5e5e5;display:flex;justify-content:center;align-items:center;height:100vh;margin:0}
            .box{text-align:center;padding:32px}
            h1{font-size:3rem;margin:0;color:#333}
            p{color:#666;margin-top:8px}
            </style></head>
            <body><div class="box"><h1>{err.status_code}</h1><p>Service unavailable — try again shortly.</p></div></body></html>
HTML
    }
}

 # DOMAINS

hq.thenairn.com {
    import proxy_error_page
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy 192.168.96.14:3777
    }
    handle {
        import admin
        reverse_proxy 192.168.96.14:3777
    }
}

claw.thenairn.com {
    import proxy_error_page
    import admin
    reverse_proxy 192.168.96.14:18790 {
        header_up Authorization "Bearer 7u88crYayckAQO9QkoW7Z8BcOFi8XLNMWRumgCviFj8"
    }
}

http://192.168.96.14 { 
    reverse_proxy go2rtc:1984 {
        flush_interval -1
    }
}

auth.thenairn.com {
    authenticate with myportal
}

transmission.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy gluetun:9091
    }
    handle {
        import admin
        reverse_proxy gluetun:9091
    }
}

http://gluetun.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy gluetun:9091
    }
    handle {
        redir https://{host}{uri}
    }
}

media.thenairn.com {
    reverse_proxy seerr:5055
} 
 
home.thenairn.com {
    reverse_proxy 192.168.96.15:8123
}

unifi.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy https://192.168.96.1 {
            transport http {
                tls_insecure_skip_verify
            }
        }
    }
    handle {
        import admin
        reverse_proxy https://192.168.96.1 {
            transport http {
                tls_insecure_skip_verify
            }
        }
    }    
}

unifi24.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy https://unifi-network-application:8443 {
            transport http {
                tls_insecure_skip_verify
            }
        }
    }
    handle {
        import admin
        reverse_proxy https://unifi-network-application:8443 {
            transport http {
                tls_insecure_skip_verify
            }
        }
    }
}

unraid.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy 192.168.96.14:6059
    }
    handle {
        import admin
        reverse_proxy 192.168.96.14:6059
    }
}

download.thenairn.com {

    handle /share/* {
        reverse_proxy admin:80
    }

    handle {
        import admin
        reverse_proxy admin:80
    }
}

radarr.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy radarr:7878
    }
    handle {
        import user
        reverse_proxy radarr:7878
    }
}

animeradarr.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy animeradarr:7878
    }
    handle {
        import user
        reverse_proxy animeradarr:7878
    }
}

http://sonarr.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy sonarr:8989
    }
    handle {
        redir https://{host}{uri}
    }
}   

sonarr.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy sonarr:8989
    }
    handle {
        import user
        reverse_proxy sonarr:8989
    }
}    

http://animesonarr.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy animesonarr:8989
    }
    handle {
        redir https://{host}{uri}
    }
}   

animesonarr.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy animesonarr:8989
    }
    handle {
        import user
        reverse_proxy animesonarr:8989
    }
}    

plex.thenairn.com {
    encode gzip
    reverse_proxy plex:32400
}

docs.thenairn.com {
    reverse_proxy paperless:8000
}

prowlarr.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy prowlarr:9696
    }
    handle {
        import user
        reverse_proxy prowlarr:9696
    }
}

dashboard.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy plugsy:3000
    }
    handle {
        import admin
        reverse_proxy plugsy:3000
    }
}

invoice.thenairn.com { 
    root * /var/www/app/public
    php_fastcgi invoiceninja:9000
    encode zstd gzip
    file_server browse
}


rclone.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy rclone:5775 {
           header_up Authorization "Basic aW5sdXN0cmE6dGhpcyBpcyBhbiByY2xvbmUgcGFzc3dvcmQ="
        }
    }
    handle {
        import admin
        reverse_proxy rclone:5775 {
           header_up Authorization "Basic aW5sdXN0cmE6dGhpcyBpcyBhbiByY2xvbmUgcGFzc3dvcmQ="
        }
    }
}

nairn.wedding {
    reverse_proxy weddingphotos:3000
}

gallery.thenairn.com {
    reverse_proxy gallery:3000
}

sync.thenairn.com {
    
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy syncthing:8384
    }
    handle {
        import admin
        reverse_proxy syncthing:8384
    }
}

immich.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy immich_server:2283
    }
    handle {
        import family
        reverse_proxy immich_server:2283
    }
}

frame.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy immich-kiosk:3000
    }
    handle {
        import family
        reverse_proxy immich-kiosk:3000
    }
}

cameras.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
      handle @internal {
          reverse_proxy go2rtc:1984 {
              flush_interval -1
          }
      }
      handle {
          import family
          reverse_proxy go2rtc:1984 {
              flush_interval -1
          }
      }
}

frigate.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy frigate:5000 {
            flush_interval -1
        }
    }
    handle {
        import family
        reverse_proxy frigate:5000 {
            flush_interval -1
        }
    }
}

multicams.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy frame-cams:3000
    }
    handle {
        import family
        reverse_proxy frame-cams:3000
    }
}

docker.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy whatsupdocker:3000
    }
    handle {
        import admin
        reverse_proxy whatsupdocker:3000
    }   
}

pikvm.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle {
        import admin
        reverse_proxy 192.168.96.95:443 {
            transport http {
                tls_insecure_skip_verify
            }
        }
    }
    handle @internal {
        reverse_proxy 192.168.96.95:443 {
            transport http {
                tls_insecure_skip_verify
            }
        }
    }
}

pdf.thenairn.com {
    reverse_proxy stirlingpdf:8080
}

manga.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle {
        import admin
        reverse_proxy suwayomi:4567 
    }
    handle @internal {
        reverse_proxy suwayomi:4567
    }
}

paperbox.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy paperbox:3000
    }
    handle {
        import admin
        reverse_proxy paperbox:3000
    }
}

syncmanga.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16
    }
    handle {
        import admin
        reverse_proxy syncyomi:8282
    }
    handle @internal {
        reverse_proxy syncyomi:8282
    }
}

metube.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy metube:8081
    }
    handle {
        import family
        reverse_proxy metube:8081
    }
}

iplayer.thenairn.com {
    tls {
        resolvers 1.1.1.1 8.8.8.8
    }
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy gluetun-uk:4404
    }
    handle {
        import admin
        reverse_proxy gluetun-uk:4404
    }
}

# backend.iplayer.thenairn.com — direct get_iplayer web UI (manual use)
backend.iplayer.thenairn.com {
    tls {
        resolvers 1.1.1.1 8.8.8.8
    }
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy gluetun-uk:1935
    }
    handle {
        import admin
        reverse_proxy gluetun-uk:1935
    }
}

# Catch-all: reject unknown subdomains (no TLS, just abort)
http://*.thenairn.com {
    abort
}
orca.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy orca:6768
    }
    handle {
        import admin
        reverse_proxy orca:6768
    }
}

# Paperclip instance running on the Mac (192.168.99.86:3100), plain HTTP on the
# LAN. Only answers while the Mac is awake — a 502 here means the Mac is asleep,
# not a config fault.
#
# Auth mirrors orca.thenairn.com exactly: LAN/Docker ranges bypass the portal,
# everything else goes through the OAuth admin policy.
#
# Do NOT add `header_up Host` — Paperclip checks Host against an allowlist and
# 403s anything it doesn't recognise. Caddy passes the original Host through by
# default, which is what we want. WebSocket upgrades need no extra config either:
# Caddy proxies them transparently, and LAN clients (the live-update UI) hit the
# @internal branch, so the auth layer is never in the upgrade path for them.
rowm.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy 192.168.99.86:3100
    }
    handle {
        import admin
        reverse_proxy 192.168.99.86:3100
    }
}

# Second Paperclip instance — the one that runs on Tower itself, as the
# `paperclip` compose service (docker-compose.paperclip.yml) with its own
# Postgres. Unlike rowm.thenairn.com (which proxies to the Mac and is only up
# while the Mac is awake), this one is local to the box and always on.
#
# Same auth shape as orca/rowm: LAN bypasses the portal, external goes through
# the OAuth admin policy. Host is passed through unrewritten — Paperclip's
# allowlist is set to rainn.thenairn.com and 403s anything else.
rainn.thenairn.com {
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy paperclip:3100
    }
    handle {
        import admin
        reverse_proxy paperclip:3100
    }
}

# Illustrated-book review + build UI for the Grace & Quentin wedding book.
# Runs inside the orca container (project: /mnt/cache/HQ/nairn-book), started by
# nairn-book/bin/serve.sh. 172.18.0.14 is orca on thenairncom_default; "orca:7790"
# resolves to the same place if the address ever drifts.
grace-wedding.thenairn.com {
    import proxy_error_page
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy 172.18.0.14:7790
    }
    handle {
        import admin
        reverse_proxy 172.18.0.14:7790
    }
}

# The book itself — the shareable artefact, and the thing the presenter's QR
# code points at. Deliberately still behind the admin portal from outside: the
# wedding has not happened and this is a surprise. To open it to guests, delete
# the `handle { import admin ... }` block and leave a single unguarded
# file_server, then reload.
# PUBLIC — no auth. Opened 2026-08-15 at Thomas's request so guests' phones can
# reach it over cellular at the wedding. Anyone with the URL can read the book.
# To close it again, restore the @internal + `import admin` pair used by
# grace-wedding-present below, and reload.
grace-wedding-book.thenairn.com {
    import proxy_error_page
    root * /Playground/grace-wedding/book
    encode zstd gzip
    file_server
}

# The guests' phones, and the state they follow.
#
# PUBLIC — no `import admin`, on purpose. Quentin is French and much of his
# family speaks little English; Thomas reads the English aloud and these phones
# carry the FRENCH, in sync, line by line. They are held by guests sitting in a
# room on cellular, and an OAuth wall would make the feature useless to exactly
# the people it exists for. Nothing here is not about to be read aloud anyway.
#
# The split matters: Caddy serves the page and the twenty-two spread JPEGs
# itself, straight off the Playground mount, so fifty phones loading a picture
# never touch the Bun process. That process only fans out a few hundred bytes of
# JSON per slide change.
grace-wedding-live.thenairn.com {
    import proxy_error_page

    # `flush_interval -1` turns off response buffering. Without it Caddy could
    # hold the SSE stream in a buffer and the whole room would sit a slide
    # behind — silently, which is the worst way for this to fail.
    handle /api/* {
        reverse_proxy gracewedding:7791 {
            flush_interval -1
        }
    }

    handle {
        root * /Playground/grace-wedding/live
        encode zstd gzip
        # ── caching, which is the difference between a primed room and a
        # stalled one ────────────────────────────────────────────────────────
        # Caddy's file_server sends Etag and Last-Modified but NO Cache-Control,
        # so a browser falls back to heuristic freshness — which for a file
        # written minutes ago is close to zero. Every phone would revalidate
        # every picture on every view: a round trip each, over the one congested
        # cell in the building, at the exact moment it matters. The page
        # prefetches all 22 pictures while guests are still sitting down, and
        # without these headers that work would be thrown away.
        #
        # The pictures are safe to pin hard because the page requests them with
        # a ?v=<content hash>, and browsers key the cache on the full URL: a
        # republish changes the query and simply misses. A week comfortably
        # covers the wedding.
        @pics path /img/*
        header @pics Cache-Control "public, max-age=604800, immutable"
        # The page and the manifest must NOT be pinned — Thomas may republish an
        # hour before he stands up. `no-cache` still allows a 304, so it is one
        # small conditional request, not a re-download.
        @fresh path / /index.html /manifest.json
        header @fresh Cache-Control "no-cache"
        file_server
    }
}

# ══ roussel.nairn.wedding ═══════════════════════════════════════════════════
#
# ONE address for the wedding, on the couple's own domain — Roussel is Quentin's
# surname. This is what goes on the QR code and what people are told out loud;
# "roussel dot nairn dot wedding" survives being said across a room in a way
# that "grace-wedding-live dot thenairn dot com" does not.
#
#   /            the guests' phones — the live follow-along        PUBLIC
#   /book/       the whole book                                    PUBLIC
#   /narrator/   the presenter, Thomas's own tool                  gated
#   /api/*       the live state service (Bun, in-memory)
#
# The live view is at the ROOT deliberately. It loads `img/NN.jpg` and
# `manifest.json` with relative paths and talks to `/api/*` as a sibling, so
# nothing has to be rewritten, and no phone ever gets a rewritten URL wrong.
#
# The grace-wedding-*.thenairn.com vhosts above serve the same three directories
# and are left exactly as they were — this is an additional front door, not a
# migration, and they are already verified.
roussel.nairn.wedding {
    import proxy_error_page

    # `flush_interval -1` turns off response buffering. Without it Caddy could
    # hold the SSE stream in a buffer and the whole room would sit a slide
    # behind — silently, which is the worst way for this to fail.
    handle /api/* {
        reverse_proxy gracewedding:7791 {
            flush_interval -1
        }
    }

    # A bare /book has no trailing slash, so `handle_path /book/*` would miss it
    # and the guest would land on the live view instead of the book. One redirect
    # each, and both spellings work.
    #
    # `?{query}` is not decoration. Without it the redirect DROPS the query, so
    # /book?full=1 became /book/ and the reader opened in its partial,
    # follow-the-speech mode instead of the whole book — which looks exactly
    # like "the reveal didn't work". A trailing bare `?` when there is no query
    # is harmless.
    redir /book /book/?{query} 308
    redir /narrator /narrator/?{query} 308

    handle_path /book/* {
        root * /Playground/grace-wedding/book
        encode zstd gzip
        # The book is a single ~7 MB self-contained file. Fifty phones fetching
        # it at the instant of the release is the worst moment to ask for it, so
        # the phone page pulls it in the background while people are seated and
        # this makes that cache worth having: `no-cache` still revalidates, but
        # a 304 reuses the copy already on the device instead of sending 7 MB
        # again.
        header Cache-Control "no-cache"
        file_server
    }

    # PUBLIC, deliberately — Thomas's call. This is the script with his private
    # notes on it, so it was gated at first, but `import admin` CANNOT work on
    # this domain: the auth portal sets `cookie domain thenairn.com`, so its
    # session cookie is never sent to nairn.wedding. The gate would bounce to
    # Google, come back, find no cookie, and loop forever. A login loop in his
    # hand two minutes before he stands up is worse than a guest reading ahead.
    #
    # So the URL simply being unadvertised is the protection. Worst case, one
    # guest guesses /narrator and sees the jokes early.
    #
    # The presenter is also a self-contained file: opening dist/present/index.html
    # off his laptop needs no network and no login at all, and remains the most
    # reliable way to run it on the night.
    handle_path /narrator/* {
        root * /Playground/grace-wedding/present
        encode zstd gzip
        file_server
    }

    # The 4K print plates, for reviewing before ordering. Gated like /narrator:
    # 113 MB of artwork has no business on a public URL a hundred guests hold,
    # and nobody needs it but Thomas and the printer.
    handle_path /plates/* {
        @lan {
            remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
        }
        handle @lan {
            root * /Playground/grace-wedding/plates
            encode zstd gzip
            file_server browse
        }
        handle {
            respond "Not here." 404
        }
    }

    handle {
        root * /Playground/grace-wedding/live
        encode zstd gzip
        # ── caching, which is the difference between a primed room and a
        # stalled one ────────────────────────────────────────────────────────
        # Caddy's file_server sends Etag and Last-Modified but NO Cache-Control,
        # so a browser falls back to heuristic freshness — which for a file
        # written minutes ago is close to zero. Every phone would revalidate
        # every picture on every view: a round trip each, over the one congested
        # cell in the building, at the exact moment it matters. The page
        # prefetches all 22 pictures while guests are still sitting down, and
        # without these headers that work would be thrown away.
        #
        # The pictures are safe to pin hard because the page requests them with
        # a ?v=<content hash>, and browsers key the cache on the full URL: a
        # republish changes the query and simply misses. A week comfortably
        # covers the wedding.
        @pics path /img/*
        header @pics Cache-Control "public, max-age=604800, immutable"
        # The page and the manifest must NOT be pinned — Thomas may republish an
        # hour before he stands up. `no-cache` still allows a 304, so it is one
        # small conditional request, not a re-download.
        @fresh path / /index.html /manifest.json
        header @fresh Cache-Control "no-cache"
        file_server
    }
}

# The live presenter — script on his device, picture on the projector. This one
# stays gated permanently; it is a tool, not an artefact.
grace-wedding-present.thenairn.com {
    import proxy_error_page
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        root * /Playground/grace-wedding/present
        encode zstd gzip
        file_server
    }
    handle {
        import admin
        root * /Playground/grace-wedding/present
        encode zstd gzip
        file_server
    }
}

playground.thenairn.com {
    tls {
        resolvers 1.1.1.1 8.8.8.8
    }
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        root * /Playground
        encode zstd gzip
        file_server browse
    }
    handle {
        import admin
        root * /Playground
        encode zstd gzip
        file_server browse
    }
}

memory.thenairn.com {
    import proxy_error_page
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    handle @internal {
        reverse_proxy 192.168.96.14:9999
    }
    handle {
        import admin
        reverse_proxy 192.168.96.14:9999
    }
}

memory-api.thenairn.com {
    import proxy_error_page
    @internal {
        remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
    }
    # Bearer token for off-LAN clients (Claude Code hooks cannot complete the
    # Google SSO flow). 256-bit random; HINDSIGHT_API_TOKEN in .env.
    @token {
        header Authorization "Bearer a5abcee9a972a9cfc90b8b46b5eab53eda147c6172d0ec65b9f2dee112c954e3"
    }
    handle @internal {
        reverse_proxy 192.168.96.14:8890
    }
    handle @token {
        reverse_proxy 192.168.96.14:8890
    }
    handle {
        import admin
        reverse_proxy 192.168.96.14:8890
    }
}
